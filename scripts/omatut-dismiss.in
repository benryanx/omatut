#!/usr/bin/env bash
set -euo pipefail

state="$(omarchy-shell omatut state 2>/dev/null || true)"
[[ -z "$state" || "$state" == "closed" ]] && exit 0
curl --silent --fail -X POST http://127.0.0.1:47841/api/overlay/dismiss >/dev/null
