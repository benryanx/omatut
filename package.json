{
  "name": "omatut",
  "version": "0.6.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "A screen-aware, context-aware tutor for Omarchy.",
  "scripts": {
    "start": "node src/server.ts",
    "test": "node --test test/*.test.ts",
    "check": "node --check src/server.ts && node --check public/app.js",
    "install:desktop": "bash scripts/install-desktop.sh"
  },
  "engines": {
    "node": ">=24"
  }
}
